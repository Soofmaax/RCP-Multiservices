# Front-end variables (Vite)
# Optional: override the default reviews endpoint (defaults to /api/google-reviews via Netlify function)
# VITE_REVIEWS_ENDPOINT=https://your-domain.example/api/google-reviews

# Netlify functions env (set these in Netlify dashboard → Site settings → Environment variables)
# GOOGLE_MAPS_API_KEY=your_google_maps_api_key
# GOOGLE_PLACE_ID=your_google_place_id